import {PostTagType} from "../../Types/PostTagType.ts";

export const PostLinkTag: PostTagType = {
    slug: 'post_link',
    title: "Post Link",
    description: "Post Link",
    defaultParams: {
        type: "post_link"
    }
}